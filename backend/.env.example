PORT=4000
# Secret used for simple single-user authentication.
APP_PASSWORD=change-me

# Your OpenAI API key (will be required later for vector store operations).
OPENAI_API_KEY=sk-your-openai-key

# OpenAI vector store ID (set after running the init endpoint).
ARGUS_VECTOR_STORE_ID=

