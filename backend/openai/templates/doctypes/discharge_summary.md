# Document Type: discharge_summary

This document is a **hospital discharge summary**.

Guidance:

- Focus on:
  - Reason for hospitalization and principal diagnoses.
  - Hospital course summarized.
  - Condition at discharge.
  - Medications at discharge, follow-up appointments, and key instructions.
- Pay particular attention to follow-up recommendations and any explicit safety-net instructions.

Constraints:

- `document_type` MUST be `"discharge_summary"` in the JSON.
- Use `follow_up_recommended` and `follow_up_timeframe` to reflect the documented plan.

